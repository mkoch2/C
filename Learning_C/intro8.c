#include <stdio.h>

int main()
{

    // Struct - collection of variables under a single name --> variable can be of different types
    struct Person
    {
        double salary;
        int age;
    };

    return 0;
}